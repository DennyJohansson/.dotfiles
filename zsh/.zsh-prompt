autoload -U colors && colors
setopt prompt_subst
bindkey -v

git_branch() {
  local b
  b=$(git symbolic-ref --short -q HEAD 2>/dev/null) \
    || b=$(git rev-parse --short HEAD 2>/dev/null) || return
  printf ' [%s]' "$b"
}

PROMPT='%F{blue}%~%f%F{yellow}$(git_branch)%f%F{green} ❯%f '
